name: Release

on:
  push:
    tags: ['v*']
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    name: Build SheetMeld.exe
    runs-on: windows-latest
    env:
      # Test scripts print Chinese; the Windows runner defaults to cp1252
      PYTHONUTF8: '1'
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # all tags, to locate the previous release for the notes

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt pyinstaller

      - name: Run tests
        run: |
          $scripts = @('test_merger.py', 'test_differ.py', 'test_updater.py', 'test_api_merge.py', 'test_regressions.py', 'test_svn_update.py')
          foreach ($script in $scripts) {
            python "tests/$script"
            if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          }

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Run frontend regressions
        run: node --test tests/test_frontend.js

      - name: Build executable
        run: >
          pyinstaller --noconfirm --onefile --noconsole
          --name SheetMeld
          --add-data "static;static"
          --hidden-import pystray._win32
          --hidden-import PIL.Image
          --hidden-import PIL.ImageDraw
          server.py

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: SheetMeld
          path: dist/SheetMeld.exe

      - name: Extract release notes from changelog
        if: startsWith(github.ref, 'refs/tags/')
        id: notes
        shell: bash
        run: |
          PREV_TAG=$(git describe --tags --abbrev=0 "${GITHUB_REF_NAME}^" 2>/dev/null || true)
          echo "Previous tag: ${PREV_TAG:-<none>}"
          python .github/release_notes.py "${GITHUB_REF_NAME}" "${PREV_TAG}" > release_notes.md
          echo "title=$(python .github/release_notes.py --title "${GITHUB_REF_NAME}")" >> "$GITHUB_OUTPUT"

      - name: Attach to release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          name: ${{ steps.notes.outputs.title }}
          files: dist/SheetMeld.exe
          body_path: release_notes.md
